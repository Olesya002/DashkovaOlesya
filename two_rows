function two_rows(M1)
k = ndims(M1);
A = size(M1);
b = A(1);
if ((b ~= 2) && (k == 2)) || ((b ~= 2) && (k ~= 2))
    M1 = zeros(A(1),A(2));
    disp("Массив должен быть двумерным и иметь две строки!");
    disp(M1);
else
    disp(M1);
end
